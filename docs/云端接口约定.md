# 云端服务接口约定 —— 仓库侧入口

**这份文件不是约定本身，是指路。** 约定有两卷，都在共享库
`.project-context/documents/`，因为它们是需求侧文档而非代码：

| 卷 | 路径 | 地位 |
|---|---|---|
| 抄录卷 | `.project-context/documents/云端服务接口约定_抄录卷_v0.1.md` | **既有事实的转录** —— 足压平台已跑通的实现，逐条带文件行号。**不需要服务方确认**，可直接照着写客户端 |
| 待确认卷 | `.project-context/documents/云端服务接口约定_待确认卷_v0.1.md` | **提案** —— 抄不到的三条。**服务方签字前不得当作约定** |

来源：RAY-353 scope `cloud-api-contract`，需求修订 R1。

## 为什么仓库里要留这一页

写客户端的人在仓库里干活，不会去翻共享库；而这两卷的**地位差别**（哪些能直接
写代码、哪些会被推翻）恰恰是最容易读漏的东西。把入口留在这里，是为了让
「照着抄录卷写」和「等待确认卷签字」这两件事不会被混成一件。

## 三条下游各自该看哪里

| Issue | 看哪一卷 | 能不能开工 |
|---|---|---|
| **RAY-355**（IngestionClient） | 抄录卷 §1 + §2；切分口径见待确认卷 §3 | **能** —— 摄取五条路由与模型都是既有事实 |
| **RAY-322**（受试者查找） | 抄录卷 §1 + §3 | **能** —— `POST /v1/subjects/resolve` 形状已定 |
| **RAY-323**（操作员登录） | 待确认卷 §1 | **不能** —— 上游没有可抄的形状，须先等服务方对 §1 表态 |

## 开工前必须知道的三个坑

1. **`E-SYN` 不是 `E-SYNC`。** 服务端 `E-SYN-409` 是*分段摘要冲突*；本仓库
   `E-SYNC`（`src/gait/app/contract.json`）是*双足同步残差*。按前缀匹配会把一次
   上传冲突显示成算法故障。**上传类错误在本仓库归 `E-NET`。**

2. **服务端错误码不能透传到界面。** 服务端是 `E-<3字母>-<3位数>`（数字即 HTTP
   状态），本仓库是四位数 + 域内号段，`errors.check_code()` 会直接拒。必须在
   客户端内部做映射，由 sidecar 出成品文案（RAY-248 验收第二条）。
   **`contract.json.codes` 里目前一个 `E-NET` 码都没有** —— 待确认卷 §2.2 给了
   取值与文案，**补登由消费它们的 Issue 在实现时做**，不属 RAY-353。

3. **两套凭据的方向与足压平台相反。** 足压平台把操作员账号登录得到的 token 直接
   当数据面凭据；PRD §19 / FR-01 要求数据面只认终端凭据、操作员账号只回答
   「谁在操作」。**照抄上游会违反 FR-01**，详见待确认卷 §1。

## 维护

改这两卷时**同时**过一遍 `.project-context/documents/派生文档索引.md`（v1.1 起
已登记它们）。注意两卷的更新触发条件不同：待确认卷跟 PRD 走，抄录卷跟
**足压平台的代码**走 —— 后者不在任何闸门的触发链上，索引 §4 D6 记了这个缺口。
