# environments — recreated per worktree, never shared or cloud-synced
.venv/
node_modules/

# build and cache
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/

# governance context link (device-local)
.project-context

# OS noise
.DS_Store

# 工程模式工装（cli/linktest、cli/v3prime）的采集产出。原始数据体量大且属于
# 实验证据 —— 归 .project-context/evidence/，不进版本库。
out/
