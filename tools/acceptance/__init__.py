"""真机验收脚本。**它们是代码，不是证据。**

每个脚本回答一组 Linear 判据，输入是 RAY-230 的现场采集（云端共享库），输出是一行
"达标 / N 条不达标"外加可选的逐格 JSON。**跑出来的数归证据库，脚本本身归这里。**

## 为什么在仓库里

RAY-328 的验收脚本住在云端证据库 —— 不进版本、不过审查、不过 lint。于是 RAY-339 改了
周期估计之后，其中一个的判据锚点整个失效、另一个直接 `KeyError` 崩掉，而这件事**过了
两个 Issue 才被发现**（RAY-343）。放进 `tools/` 之后它们至少受 lint 与审查约束，也能
被 `tests/test_acceptance_suite.py` 钉住接口。

## 写这些脚本的两条规矩

1. **钉性质，别钉绝对数。** RAY-328 的 `alternation_acceptance.py` 里，账目恒等式
   `槽 = 检出 − 合并 + 补槽` 至今成立，而同一个文件里的 `CYCLES_AFTER_L1` 对照表整个
   过时 —— 12 条失败全出自它。非要有基线不可，就**当场重算**，别抄成常量。
   唯一该钉死的数是 `TRUTH_CYCLES = 38`：它是现场数出来并受控的，不随代码变。
2. **每个脚本带阳性对照。** 向真机数据注入一个它本该拦住的缺陷，它必须红。没有这一条，
   "钉性质"很容易变成"永远绿" —— 而那比过时更糟，它看起来在守。

## 怎么跑

    python tools/run_acceptance.py <采集目录>...      # 全部
    PYTHONPATH=tools python -m acceptance.<名字> ...  # 单个

**不进 **`./dev`：它们要读云端数据（会把一个离线可跑的门变成要联云的），而且每个都要
跑完 24 格的完整管线。约定是"动了共享路径（周期估计、时基、检测器）就跑一遍"，
写在 `AGENTS.md` 的交付流程里。
"""
